<p id="notice"><%= notice %></p>

<div class='d-flex'>
  <div class='jumbotron jumbotron-fluid'>
    <div class='container'>
      <p><h2>Welcome to my portfolio!</h2></p>
      <p>Hello, my name is Chris Hayter and this portfolio site was created as project for a course</p>
      <p>at University of Colorado Colorado Springs in Fall 2021. This site host a few projects I have worked on.</p>
      <p>Enjoy!</p>
    </div>
  </div>
</div>

<div class='container'>
  <h2>Projects</h2>
  <% @projects.each do |project| %> 
  <div class='row'>      
    <!-- how to make them like boxes next to each other? -->
    <div class='card'>
      <div class='card-body'>
        <p><h4><%= project.title %></h4></p>
        <p><%= project.description %></p>
        <td><%= link_to 'Show', project %></td>
        <% if user_signed_in? %>
          <td><%= link_to 'Edit', edit_project_path(project) %></td>
          <td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </div>
    </div>
  </div>
<% end %>
</div>